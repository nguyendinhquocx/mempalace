---
description: Score how well organized your memory palace is, then run a guided repair session — one decision at a time.
---

Invoke the `mempalace` skill from this plugin and run the `audit` instructions, then follow them.

Concretely: run `mempalace instructions audit` in a terminal, then carry out the steps it prints.
